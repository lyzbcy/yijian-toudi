# 2026-07-25 · 腾讯闭环深化 + 复查 P0/P1 修复（v0.3 阶段一）

## 背景

v0.3 地基（多段简历、多份简历、能力矩阵、adapter registry）完成后，派子代理做了一次严格代码复查，抓到 1 个 P0（备份恢复丢数据）+ 4 个 P1（并发覆盖/脏状态）+ 若干 P2。本页记录这些修复，以及腾讯字段差异报告（design §4.2）的落地。

## 已完成

### 复查 P0/P1 修复（全部带回归测试）

- **P0-1 备份恢复丢数据**：旧版备份（v0.2 扁平 resume，无 profiles）恢复后，syncResumeActiveView 把顶层经历当空模板清空。修法：抽出 `migrateFlatResumeToProfiles` 纯函数，restoreBackupFromFile 在 update 前先调它重建 profiles。回归测试覆盖「v0.2 备份恢复后经历仍在」。
- **P1-1 applyResumeEdit 并发覆盖**：前端旧快照的 profiles 会覆盖后端并发改过的其他 profile。修法：activeProfileId 和非 active profile 一律以后端（currentResume）为权威，只把 incoming 顶层字段写进 active。回归测试覆盖「B 被并发改成 B2，前端拿旧快照 B1 保存，B 仍是 B2」。
- **P1-2 activeProfileId 被前端覆盖**：同 P1-1 一并修复。
- **P1-4 脏 activeProfileId**：syncResumeActiveView 现在会修正指向不存在 profile 的 activeProfileId（回退到 profiles[0]）。
- **P2-4 死代码**：删掉 renderCompanyCard 的 #companyGrid legacy 分支。
- **P2-6 段序号混用**：多段经历 keywords 的段序号统一用阿拉伯数字（第2段/第3段…），避免 adapter 匹配时中文/阿拉伯不一致。

### #17 腾讯字段差异报告（design §4.2）

把腾讯简历填写从「模糊关键词填表」升级成三步可复核流程：

1. **inspectTencentResume**（tencent-fill.cjs）：打开腾讯简历页，读出所有可识别表单控件（label/类型/当前值/选项），只读不写。新增 `INSPECT_FORM_FIELDS` 页面脚本。
2. **planTencentResumePatch**（tencent-fill.cjs，纯函数）：对比本地简历值和 inspect 读到的远端字段，生成四元组差异清单：
   - `action: fill`（本地有值、远端空或不同，且匹配到字段）
   - `action: skip`（本地与远端一致）
   - `action: manual`（找不到匹配字段，或 select 选项不含本地值）
   - 每条带 `localValue/remoteValue/risk/matchedField/segmentLabel`
3. **fillTencentResume**：保持原填写逻辑，填写后再读一次远端字段生成 patchPlan，随结果返回。

前端：填写完成后若有 patchPlan，弹 `#resumeReportDialog` 展示差异表（本地字段/本地值/腾讯当前值/动作/匹配与风险），明确标注「软件没有点击保存」。

### 测试覆盖

- planTencentResumePatch：fill/skip/manual 三类差异、无远端字段全 manual、覆盖风险标注，3 个专项
- 旧 schema 迁移、备份恢复不丢数据、并发覆盖、脏 activeProfileId 修正，4 个回归

## 验证（全绿）

- 单元测试 49/49
- 静态检查通过
- UI 冒烟 + 多段简历 + 多份简历 + 能力矩阵 4 个专项全过

## 诚实边界

- **T1.1 前端差异表无法做真实 UI 测试**：需要腾讯登录态才能跑到 fillResume 返回 patchPlan。纯函数 planTencentResumePatch 有完整测试，前端渲染逻辑（showResumeReport）靠代码审查 + CSS 静态检查。真实账号验收是 design §9 阶段一的人工验收项。
- **复查 P1-3（双击重命名触发两次 saveResume）未修**：影响是浪费两次磁盘写 + 可能强制保存草稿，不丢数据。计划下个迭代把重命名改成 label 上的独立小图标按钮。
- **复查 P2-2（migrate 静默覆盖用户改过的 capabilities）未修**：目前没有 UI 让用户编辑 capabilities，所以不触发。一旦加「人工标记能力」UI 再处理。
- **复查 P2-5（HTML 版本号硬编码 v0.2.0）未修**：v0.3 正式发布前统一改。

## 下一步

- T1.2 验证码集成（纯 JS 重写 OpenCV 缺口识别 + 拟人轨迹，移到 Electron workspace）
- T1.3 腾讯投递状态刷新
- T1.4 购物车前置检查（能力矩阵驱动）
- T2 五家补齐（字节/小米抽 _feishu-base，京东/美团/百度补 prepareApplication）
